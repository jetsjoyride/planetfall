rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // PUBLIC TEST MODE
    // Allow read/write access to all documents to debug permission errors.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
